# 音乐资源目录

公开仓库不包含音乐文件。仅在确认拥有公开传播授权后，按以下文件名放入 MP3：

- `远航星的告别.mp3`
- `不散的夏之灯.mp3`
- `纸飞机.mp3`
- `小小奇迹.mp3`
- `星炬不熄.mp3`
- `自无垠处归航之星.mp3`

文件补齐后，将 `app.js` 中的 `MEDIA_ASSETS_ENABLED` 改为 `true`，播放器才会加载这些资源。
